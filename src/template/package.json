{
  "name": "blog-name",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "",
  "type": "module",
  "main": "./src/index.html",
  "scripts": {
    "build": "vite build && blazed build",
    "dev": "nodemon --watch src --ext ts,json,css,html --exec \"npm run build && vite preview\""
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "vite": "^7.2.2"
  }
}
